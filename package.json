{
  "name": "casino-chip-manager",
  "version": "0.1.0",
  "homepage": "https://github.com/kamatadaiki/casino-chip-manager",
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "predeploy": "npm run build",
    "deploy": "gh-pages -d build"
  },
  "devDependencies": {
    "gh-pages": "^4.0.0"
  }
}
